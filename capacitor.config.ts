import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mukosoft.app",
  appName: "mukosoft",
  webDir: "dist/apps/mukosoft",
  bundledWebRuntime: false,
};

export default config;
